# Общие константы API-тестов.
# Вынесены из conftest.py в отдельный модуль с уникальным именем,
# чтобы избежать неоднозначности `import conftest` при одновременном
# запуске нескольких наборов (tests/api/ и tests/integration/ имеют
# разные conftest.py, но `import conftest` без квалификации может
# разрешиться в любой из них в зависимости от sys.path).

# Посевной zip-отчёт, гарантированно присутствующий на засеянном стенде.
# Шифр 1, ДопШифр TST → нормализованное имя на диске 00001-TST.zip.
SAMPLE_REPORT = "00001-TST"
